// <auto-generated />
#pragma warning disable CS0105
using HackU_2024_server.DataBase;
using MasterMemory.Validation;
using MasterMemory;
using MessagePack;
using System.Collections.Generic;
using System.Net.WebSockets;
using System.Net;
using System;
using HackU_2024_server.Tables;

namespace HackU_2024_server
{
   public sealed class ImmutableBuilder : ImmutableBuilderBase
   {
        MemoryDatabase memory;

        public ImmutableBuilder(MemoryDatabase memory)
        {
            this.memory = memory;
        }

        public MemoryDatabase Build()
        {
            return memory;
        }

        public void ReplaceAll(System.Collections.Generic.IList<ClientData> data)
        {
            var newData = CloneAndSortBy(data, x => x.GlobalUserId, System.StringComparer.Ordinal);
            var table = new ClientDataTable(newData);
            memory = new MemoryDatabase(
                table
            
            );
        }

        public void RemoveClientData(string[] keys)
        {
            var data = RemoveCore(memory.ClientDataTable.GetRawDataUnsafe(), keys, x => x.GlobalUserId, System.StringComparer.Ordinal);
            var newData = CloneAndSortBy(data, x => x.GlobalUserId, System.StringComparer.Ordinal);
            var table = new ClientDataTable(newData);
            memory = new MemoryDatabase(
                table
            
            );
        }

        public void Diff(ClientData[] addOrReplaceData)
        {
            var data = DiffCore(memory.ClientDataTable.GetRawDataUnsafe(), addOrReplaceData, x => x.GlobalUserId, System.StringComparer.Ordinal);
            var newData = CloneAndSortBy(data, x => x.GlobalUserId, System.StringComparer.Ordinal);
            var table = new ClientDataTable(newData);
            memory = new MemoryDatabase(
                table
            
            );
        }

    }
}